# Example environment variables for running the Next.js and Python apps

# PostgreSQL connection string for the Next.js app
DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# Flask secret key for the Python backend
SECRET_KEY=your-secret-key

# Optional SQLite or SQLAlchemy database URI for the Python backend
DATABASE_URI=sqlite:///database.db
